# Phase Archive

The old phase-by-phase `.planning/phases/*` documents were removed during workflow cleanup.

Reason:

- they described earlier GSD execution state
- they drifted from the active `specs/*` feature artifacts
- `.planning/` is now treated as orchestration only, not feature truth

Use these sources instead:

- `.specify/memory/constitution.md`
- `specs/*/spec.md`
- `specs/*/plan.md`
- `specs/*/tasks.md`

If a new execution phase summary is needed, it should be written as a short derivative note that references the active feature spec explicitly.

